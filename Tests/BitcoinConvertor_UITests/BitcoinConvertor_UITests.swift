import XCTest
@testable import BitcoinConvertor_UI

final class BitcoinConvertor_UITests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(BitcoinConvertor_UI().text, "Hello, World!")
    }
}
